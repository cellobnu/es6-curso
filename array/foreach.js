const logger = require("../logger");

// /** Array.protopype.foreach */

const studantes = ["marcelo", "Vini", "Roberto", "Marcos"];

studantes.forEach(function(studante) {
  console.log("Hi " + studante);
});
