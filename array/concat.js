const logger = require("../logger");

// /** Array.protopype.cocat */

const names = ["Marcos", "Vini"];
const names2 = ["Marcelo", "Roberto"];

const allNames = names.concat(names2);

logger.info(allNames);
